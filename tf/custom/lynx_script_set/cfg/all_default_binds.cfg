//This file is for all "Default binds" for each class. If one class changes something, this file will be executed to put it back to default

bind z voice_menu_1 //Medic binds this to randomvoice alias
bind r +reload //Medic binds this to fake uber
bind f +inspect //Medic binds this to negative
bind x voice_menu_2 //Medic overrides this to fake uber call
bind space +jump //Fixing jump bind
